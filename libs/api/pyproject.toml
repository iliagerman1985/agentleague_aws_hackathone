[project]
name = "api"
description = ""
requires-python = ">=3.13"
version = "0.1.0"
dependencies = [
   "common",
]

[dependency-groups]
dev = [
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["api"]
